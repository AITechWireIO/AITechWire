APPLIED and compiles. New staging block:
        # B2-STAGE: stage only this run's own output. "git add -A" committed
        # whatever else sat in the tree, including articles the gate rejected.
        _sslug = filename.rsplit('/', 1)[-1]
        if _sslug.endswith('.html'):
            _sslug = _sslug[:-5]
        cmds = [
            f"cd {SITE_DIR}",
            "git add -u",
            f'git add "{section}/{filename}"',
            f"git add images/{_sslug}-hero.jpg images/{_sslug}-og.jpg 2>/dev/null; true",
            f'git commit -m "📰 {today_str()}: {headline[:60]}" --allow-empty',
            "git push origin main"
        ]
remaining "git add -A" in the pipeline: 2