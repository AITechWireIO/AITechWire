== every line mentioning add -A, with context ==
1063:        # of the repo before staging, so `git add -A` cannot sweep it live.
1113:        # B2-STAGE: stage only this run's own output. "git add -A" committed
== any remaining add -A as an actual COMMAND anywhere in scripts? ==
/home/ubuntu/.openclaw/workspace/scripts/aitechwire_daily.py:1113:        # B2-STAGE: stage only this run's own output. "git add -A" committed
== gate still passes ==
ok   HEADER ok — every article carries the one canonical menu
  ok   PREVIEW ok — every article has a card X can fetch
  ok   INDEX ok — every listed article exists (88 entries)
  ok   TODAY ok — one article dated today
  ok   LINKS ok — every internal link resolves
  ok   REPEAT ok - today's story is not one of the last 7 days

PUBLISH CONTRACT SATISFIED — clear to publish.